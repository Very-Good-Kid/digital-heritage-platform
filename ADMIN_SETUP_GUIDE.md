# 管理员注册指南

## 目录
1. [概述](#一概述)
2. [准备工作](#二准备工作)
3. [方法一：使用脚本创建管理员](#三方法一使用脚本创建管理员)
4. [方法二：通过注册页面创建](#四方法二通过注册页面创建)
5. [方法三：直接修改数据库](#五方法三直接修改数据库)
6. [验证管理员权限](#六验证管理员权限)
7. [常见问题](#七常见问题)
8. [安全建议](#八安全建议)

---

## 一、概述

### 1.1 管理员权限说明

管理员账号拥有以下权限：
- ✅ 访问后台管理系统 (`/admin`)
- ✅ 查看所有用户的数据
- ✅ 管理用户（创建、编辑、删除）
- ✅ 管理数字资产
- ✅ 管理数字遗嘱
- ✅ 管理平台政策
- ✅ 管理FAQ
- ✅ 管理故事墙
- ✅ 查看统计数据

### 1.2 默认管理员信息

**重要提示：** 系统默认没有创建管理员账号，您需要手动创建第一个管理员。

---

## 二、准备工作

### 2.1 确保应用已启动

```bash
# 进入项目目录
cd c:\Users\admin\Desktop\demo - codebuddy

# 安装依赖（如果还未安装）
pip install -r requirements.txt

# 启动应用
python app.py
```

应用将在 `http://localhost:5000` 启动。

### 2.2 确保数据库已初始化

首次访问应用时，数据库会自动初始化。如果数据库未初始化，请访问首页：
```
http://localhost:5000
```

### 2.3 准备管理员账号信息

在创建管理员之前，请准备好以下信息：
- **用户名**：建议使用英文字母和数字，如 `admin`
- **邮箱**：有效的邮箱地址，如 `admin@example.com`
- **密码**：强密码（至少8位，包含大小写字母、数字和特殊字符）

**密码示例：** `Admin@2024!`

---

## 三、方法一：使用脚本创建管理员（推荐）

### 3.1 使用create_admin.py脚本

项目提供了专门的管理员创建脚本 `create_admin.py`。

**步骤：**

1. **打开命令行终端**

2. **运行创建脚本**
   ```bash
   cd c:\Users\admin\Desktop\demo - codebuddy
   python create_admin.py
   ```

3. **按照提示输入信息**
   ```
   输入用户名: admin
   输入邮箱: admin@example.com
   输入密码: Admin@2024!
   确认密码: Admin@2024!
   ```

4. **查看创建结果**
   ```
   创建成功！
   用户名: admin
   邮箱: admin@example.com
   管理员权限: 已授予
   ```

### 3.2 脚本源码说明

`create_admin.py` 脚本的实现原理：

```python
from app import app, db, User
from werkzeug.security import generate_password_hash

def create_admin():
    """创建管理员账号"""
    with app.app_context():
        username = input("输入用户名: ")
        email = input("输入邮箱: ")
        password = input("输入密码: ")
        confirm_password = input("确认密码: ")

        if password != confirm_password:
            print("密码不一致！")
            return

        # 检查用户名是否已存在
        if User.query.filter_by(username=username).first():
            print("用户名已存在！")
            return

        # 检查邮箱是否已存在
        if User.query.filter_by(email=email).first():
            print("邮箱已被注册！")
            return

        # 创建管理员用户
        user = User(
            username=username,
            email=email,
            password_hash=generate_password_hash(password),
            is_admin=True,
            is_active=True
        )

        db.session.add(user)
        db.session.commit()

        print("创建成功！")
        print(f"用户名: {username}")
        print(f"邮箱: {email}")
        print("管理员权限: 已授予")

if __name__ == '__main__':
    create_admin()
```

---

## 四、方法二：通过注册页面创建

### 4.1 注册普通用户

1. **访问注册页面**
   ```
   http://localhost:5000/register
   ```

2. **填写注册信息**
   - 用户名：`admin`
   - 邮箱：`admin@example.com`
   - 密码：`Admin@2024!`
   - 确认密码：`Admin@2024!`

3. **点击注册按钮**

4. **登录账号**
   ```
   http://localhost:5000/login
   ```

### 4.2 提升为管理员

注册成功后，需要将普通用户提升为管理员。

**步骤：**

1. **启动Python REPL**
   ```bash
   python
   ```

2. **执行以下代码**
   ```python
   from app import app, db, User

   with app.app_context():
       # 查找用户
       user = User.query.filter_by(username='admin').first()

       if user:
           # 设置为管理员
           user.is_admin = True
           db.session.commit()

           print(f"用户 {user.username} 已设置为管理员")
       else:
           print("用户不存在")
   ```

3. **退出REPL**
   ```python
   exit()
   ```

### 4.3 验证管理员权限

1. **退出当前登录**（如果已登录）

2. **重新登录**
   ```
   http://localhost:5000/login
   ```

3. **访问后台管理系统**
   ```
   http://localhost:5000/admin
   ```

如果成功跳转到后台管理系统，说明管理员权限设置成功。

---

## 五、方法三：直接修改数据库

### 5.1 使用SQLite命令行

**步骤：**

1. **打开命令行终端**

2. **进入数据库目录**
   ```bash
   cd c:\Users\admin\Desktop\demo - codebuddy\instance
   ```

3. **打开SQLite数据库**
   ```bash
   sqlite3 digital_heritage.db
   ```

4. **查看用户表**
   ```sql
   SELECT id, username, email, is_admin FROM users;
   ```

5. **将用户设置为管理员**
   ```sql
   UPDATE users SET is_admin = 1 WHERE username = 'admin';
   ```

6. **验证修改**
   ```sql
   SELECT id, username, email, is_admin FROM users;
   ```

7. **退出SQLite**
   ```sql
   .quit
   ```

### 5.2 使用数据库管理工具

推荐使用以下工具：
- **DB Browser for SQLite**（Windows/Mac/Linux）
- **DBeaver**（跨平台）
- **SQLiteStudio**（Windows）

**使用DB Browser for SQLite的步骤：**

1. **下载并安装DB Browser for SQLite**
   - 官网：https://sqlitebrowser.org/

2. **打开数据库文件**
   - 文件路径：`c:\Users\admin\Desktop\demo - codebuddy\instance\digital_heritage.db`

3. **浏览数据**
   - 点击 "浏览数据" 标签
   - 选择 `users` 表

4. **修改管理员权限**
   - 找到目标用户
   - 将 `is_admin` 字段从 `0` 改为 `1`
   - 点击 "保存"

5. **关闭工具**

---

## 六、验证管理员权限

### 6.1 登录测试

1. **访问登录页面**
   ```
   http://localhost:5000/login
   ```

2. **输入管理员账号**
   - 用户名：`admin`
   - 密码：`Admin@2024!`

3. **点击登录**

### 6.2 访问后台管理系统

1. **访问后台首页**
   ```
   http://localhost:5000/admin
   ```

2. **检查是否成功跳转**
   - ✅ 成功：显示后台管理系统仪表盘
   - ❌ 失败：显示"需要管理员权限"错误

### 6.3 测试管理员功能

1. **查看用户管理**
   ```
   http://localhost:5000/admin/users
   ```

2. **查看FAQ管理**
   ```
   http://localhost:5000/admin/content/faqs
   ```

3. **查看统计数据**
   ```
   http://localhost:5000/admin/
   ```

如果所有页面都能正常访问，说明管理员账号创建成功。

---

## 七、常见问题

### Q1：脚本运行失败，提示"模块未找到"

**原因：** Python依赖未安装

**解决方案：**
```bash
pip install -r requirements.txt
```

### Q2：脚本运行失败，提示"数据库未初始化"

**原因：** 数据库表未创建

**解决方案：**
1. 访问首页初始化数据库：`http://localhost:5000`
2. 或手动创建数据库：
   ```python
   from app import app, db
   with app.app_context():
       db.create_all()
   ```

### Q3：创建成功后无法登录

**原因：** 密码输入错误或账号未激活

**解决方案：**
1. 检查用户名和密码是否正确
2. 检查账号是否激活（`is_active` 字段应为 `True`）
3. 清除浏览器缓存后重试

### Q4：无法访问后台管理系统

**原因：** 管理员权限未正确设置

**解决方案：**
1. 使用方法二或方法三重新设置管理员权限
2. 检查 `is_admin` 字段是否为 `True`（或 `1`）

### Q5：忘记管理员密码

**解决方案：**
创建新的管理员账号，或重置旧密码：

```python
from app import app, db, User
from werkzeug.security import generate_password_hash

with app.app_context():
    user = User.query.filter_by(username='admin').first()
    user.password_hash = generate_password_hash('NewPassword@2024!')
    db.session.commit()
```

---

## 八、安全建议

### 8.1 密码安全

**建议：**
- ✅ 使用强密码（至少12位）
- ✅ 包含大小写字母、数字和特殊字符
- ✅ 定期更换密码（每3-6个月）
- ✅ 不要使用常见密码
- ✅ 不要与其他网站使用相同密码

**密码示例：**
```
Admin@2024!Secure#
MyAdmin#Pass$2024
D1g1tal@Heritage#2024
```

### 8.2 账号安全

**建议：**
- ✅ 使用专业的邮箱地址
- ✅ 开启邮箱的两步验证
- ✅ 不要共享管理员账号
- ✅ 定期检查登录日志
- ✅ 发现异常立即修改密码

### 8.3 系统安全

**建议：**
- ✅ 定期更新Python依赖
- ✅ 及时修复安全漏洞
- ✅ 启用HTTPS（生产环境）
- ✅ 定期备份数据库
- ✅ 限制管理员账号数量

### 8.4 生产环境部署

**部署到生产环境时的注意事项：**

1. **修改SECRET_KEY**
   ```python
   # config.py
   import secrets
   SECRET_KEY = secrets.token_hex(32)
   ```

2. **使用环境变量**
   ```bash
   export SECRET_KEY="your-secret-key"
   export FLASK_ENV="production"
   ```

3. **启用HTTPS**
   - Render自动配置HTTPS
   - 或使用Let's Encrypt

4. **限制访问**
   - 使用防火墙
   - 配置IP白名单（可选）

5. **定期监控**
   - 查看访问日志
   - 监控异常行为
   - 设置告警

---

## 九、快速参考

### 9.1 创建管理员命令速查

**方法一：使用脚本**
```bash
python create_admin.py
```

**方法二：注册后提升权限**
```python
from app import app, db, User
with app.app_context():
    user = User.query.filter_by(username='admin').first()
    user.is_admin = True
    db.session.commit()
```

**方法三：直接修改数据库**
```bash
sqlite3 instance/digital_heritage.db
UPDATE users SET is_admin = 1 WHERE username = 'admin';
```

### 9.2 重置密码命令速查

```python
from app import app, db, User
from werkzeug.security import generate_password_hash

with app.app_context():
    user = User.query.filter_by(username='admin').first()
    user.password_hash = generate_password_hash('NewPassword@2024!')
    db.session.commit()
```

### 9.3 重要URL

| 功能 | URL |
|------|-----|
| 首页 | `http://localhost:5000` |
| 注册 | `http://localhost:5000/register` |
| 登录 | `http://localhost:5000/login` |
| 用户仪表盘 | `http://localhost:5000/dashboard` |
| 后台管理系统 | `http://localhost:5000/admin` |
| 用户管理 | `http://localhost:5000/admin/users` |
| FAQ管理 | `http://localhost:5000/admin/content/faqs` |

---

## 十、联系支持

如果遇到问题，请联系：
- 项目文档：查看 `README.md` 和 `DEPLOYMENT_GUIDE.md`
- Flask文档：https://flask.palletsprojects.com/
- SQLite文档：https://www.sqlite.org/docs.html

---

**祝您使用愉快！** 🎉
